diff --git a/.env.example b/.env.example
index 8cad06e..2c7931c 100644
--- a/.env.example
+++ b/.env.example
@@ -4,7 +4,8 @@ REGION=asia-south1
 
 # ── Google APIs ───────────────────────────────────────────────────────────────
 GOOGLE_MAPS_API_KEY=your-maps-api-key
-VERTEX_LOCATION=us-central1          # Gemini 1.5 Flash region
+GEMINI_API_KEY=your-aistudio-key       # free key from aistudio.google.com
+GEMINI_MODEL=gemini-2.5-flash
 
 # ── Firestore ─────────────────────────────────────────────────────────────────
 FIRESTORE_COLLECTION_ZONES=zones
diff --git a/api/services/gemini_assistant.py b/api/services/gemini_assistant.py
index 6257a6d..3ff55b8 100644
--- a/api/services/gemini_assistant.py
+++ b/api/services/gemini_assistant.py
@@ -1,5 +1,5 @@
 """
-VenueFlow AI Assistant powered by Google Vertex AI (Gemini 1.5 Flash).
+VenueFlow AI Assistant powered by Google AI Studio (Gemini Flash, free tier).
 
 Answers natural-language questions about the venue by injecting
 live crowd density data as context into every prompt.
@@ -41,36 +41,38 @@ async def ask_gemini(
     Send a question to Gemini 1.5 Flash with live venue crowd context.
     Falls back to rule-based answers if Vertex AI is unavailable.
     """
-    try:
-        # Lazy import — only loads when first AI question is asked
-        # This is why startup is fast even with a heavy AI package
-        import vertexai
-        from vertexai.generative_models import GenerativeModel, GenerationConfig
-
-        project  = os.environ.get("GCP_PROJECT", "")
-        location = os.environ.get("VERTEX_LOCATION", "us-central1")
+    api_key = os.environ.get("GEMINI_API_KEY", "")
+    if not api_key:
+        logger.info("GEMINI_API_KEY not set — using rule-based fallback")
+        return _rule_based_fallback(user_question, zone_context)
 
-        vertexai.init(project=project, location=location)
+    try:
+        # Lazy import keeps Cloud Run cold start fast (~1s).
+        # Free Google AI Studio Developer API — no Vertex, no billing.
+        from google import genai
+        from google.genai import types
 
-        system = _build_system_prompt(wheelchair_mode, language)
-        model  = GenerativeModel(model_name="gemini-1.5-flash-001",
-                                 system_instruction=system)
+        client = genai.Client(api_key=api_key)
 
+        system  = _build_system_prompt(wheelchair_mode, language)
         context = _format_zone_context(zone_context)
         prompt  = f"{context}\n\nAttendee question: {user_question}"
 
-        config = GenerationConfig(temperature=0.2, max_output_tokens=150, top_p=0.8)
-
-        import asyncio
-        loop = asyncio.get_event_loop()
-        text = await loop.run_in_executor(
-            None,
-            lambda: model.generate_content(prompt, generation_config=config).text.strip()
+        model_name = os.environ.get("GEMINI_MODEL", "gemini-2.5-flash")
+        resp = await client.aio.models.generate_content(
+            model=model_name,
+            contents=prompt,
+            config=types.GenerateContentConfig(
+                system_instruction=system,
+                temperature=0.2,
+                max_output_tokens=150,
+                top_p=0.8,
+            ),
         )
-        return text
+        return (resp.text or "").strip() or _rule_based_fallback(user_question, zone_context)
 
     except ImportError:
-        logger.info("vertexai not installed — using fallback")
+        logger.info("google-genai not installed — using fallback")
         return _rule_based_fallback(user_question, zone_context)
     except Exception as exc:
         logger.error("Gemini call failed: %s", exc)
diff --git a/requirements.txt b/requirements.txt
index 720e181..2224bbc 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -5,7 +5,7 @@ google-cloud-firestore==2.15.0
 google-cloud-pubsub==2.21.1
 google-cloud-bigquery==3.20.1
 google-cloud-translate==3.15.3
-google-cloud-aiplatform==1.47.0
+google-genai==1.9.0
 slowapi==0.1.9
 python-dotenv==1.0.1
 httpx==0.27.0
